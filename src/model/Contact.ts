export interface Contact {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    tags: string[];
    metadata: object;
    createdDate: string;
    updatedDate: string;
    createdBy: string;
}
