export interface Contact {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    tags: string[];
    metadata: Object;
    createdDate: string;
    updatedDate: string;
}
