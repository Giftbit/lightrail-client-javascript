import {LightrailResponse} from "../LightrailResponse";
import {WebhookSecret} from "../../model/WebhookSecret";

export interface CreateWebhookSecretResponse extends LightrailResponse<WebhookSecret> {
}
